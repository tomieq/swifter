name: compability-workflow
run-name: Build for multiple Swift versions
on: [push]
jobs:
  swift-6_0-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:6.0 swift build
  swift-5_10-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:5.10 swift build
  swift-5_9-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:5.9 swift build
  swift-5_8-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:5.8 swift build
  swift-5_7-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:5.7 swift build
  swift-5_6-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:5.6 swift build
  swift-5_5-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:5.5 swift build
  swift-5_4-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker run --rm -v "$PWD:/code" -w /code swift:5.4 swift build